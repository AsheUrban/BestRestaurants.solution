@* @{
  Layout = "_Layout";
}

@using BestRestaurants.Models;
@model List<BestRestaurants.Models.Diner>;

<h1>DINERS</h1>
<hr />
<div class="card">

@if (@Model.Count == 0)
{
  <h3>No Diners have been added yet!</h3>
}

@foreach (Diner diner in Model)
{
  <li>@Html.ActionLink($"{diner.Name}", "Details", new { id = diner.DinerId })</li>
}
</div> *@
@* <hr />
<p class="fancyBtn">@Html.ActionLink("Add Restaurant", "Create", "Restaurants")</p>
<p class="fancyBtn">@Html.ActionLink("Add Cuisine", "Create", "Cuisines")</p> *@